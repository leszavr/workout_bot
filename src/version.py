"""Версия приложения — один источник истины для всех компонентов образа.

Backend, Telegram Gateway и любой будущий worker собираются из одного пакета,
поэтому их `version` совпадает. Это не требование архитектуры: Component
Registry сравнивает версии компонентов независимо, и после выделения Gateway
в отдельный репозиторий/образ здесь останется только версия backend.

Build SHA сюда не попадает: он неизвестен на момент написания кода и
приходит из окружения сборки (`BUILD_SHA`), см. `src/infrastructure/config.py`.
"""
from __future__ import annotations

APP_VERSION = "2.2.0"
