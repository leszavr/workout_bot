"""Версии компонентов.

`APP_VERSION` — версия Backend и одновременно версия образа: Backend, Gateway и
worker пока собираются из одного пакета.

Версии Gateway и worker объявлены отдельно намеренно. Component Registry
сравнивает их независимо, и после выноса Gateway за сетевую границу они меняются
по разным причинам: правка polling не касается Backend, а изменение генерации не
касается Gateway. Совместимость определяет `contract_version`, а не совпадение
версий — иначе любое обновление Backend требовало бы переразвёртывания EU.

Git SHA сюда не попадает: он неизвестен на момент написания кода, приходит из
окружения сборки (`BUILD_SHA`) и служит только трассировке. Критерием
совместимости он не является.
"""
from __future__ import annotations

APP_VERSION = "2.3.0"

# Gateway транспортирует и отображает; его версия меняется при правках работы с
# Telegram, а не при изменениях предметной логики.
GATEWAY_VERSION = "1.0.0"

# Worker выполняет повторы и recovery (Phase 1.2-D).
WORKER_VERSION = "1.0.0"
