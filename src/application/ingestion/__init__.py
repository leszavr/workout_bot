"""Пакет ingestion внешних источников знаний об упражнениях.

Слой существует отдельно от `src/application/equipment`, потому что решает другую
задачу: equipment-слой нормализует оборудование уже существующих упражнений, а
этот — решает, попадёт ли внешняя запись в canonical каталог, и что именно из неё
будет взято.

Canonical каталог остаётся один. Ни один модуль этого пакета не создаёт второй
сущности упражнения и не является runtime-зависимостью приложения: генерация
программ читает только `exercises` и базу знаний об оборудовании.
"""
